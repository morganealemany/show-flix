{% extends 'backoffice/layout.html.twig' %}

{% block title %}Saison{% endblock %}

{% block content %}
    <h1>Saison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>SeasonNumber</th>
                <td>{{ season.seasonNumber }}</td>
            </tr>
            <tr>
                <th>PublishedAt</th>
                <td>{{ season.publishedAt ? season.publishedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ season.createdAt ? season.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ season.updatedAt ? season.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('backoffice_season_index') }}">Retour à la liste</a>

    <a class="btn btn-success" href="{{ path('backoffice_season_edit', {'id': season.id}) }}">Editer</a>

    {{ include('backoffice/season/_delete_form.html.twig') }}
{% endblock %}
